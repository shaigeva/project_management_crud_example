PROJECT MANAGEMENT CRUD - ARCHITECTURE OVERVIEW
===============================================


                    ┌────────────────────────────┐
                    │    External Client/App     │
                    │   (Browser, Mobile, CLI)   │
                    └────────────┬───────────────┘
                                 │
                                 │ HTTP Requests/Responses
                                 │ (JSON)
                                 ▼
    ╔════════════════════════════════════════════════════╗
    ║           🧪 HTTP/API Tests (tests/api/)          ║
    ╠════════════════════════════════════════════════════╣
    ║                                                    ║
    ║            API Layer (FastAPI)                     ║
    ║                                                    ║
    ║    RESTful endpoints, validation, serialization    ║
    ║                                                    ║
    ╚══════════════════════┬═════════════════════════════╝
                           │
                           │ Immutable Domain Models (simple data structures only!)
                           │ (User, Project, Epic, Task)
                           │ CreateCommand, UpdateCommand
                           ▼
    ╔════════════════════════════════════════════════════╗
    ║      🧪 Repository Tests (tests/dal/)              ║
    ╠════════════════════════════════════════════════════╣
    ║                                                    ║
    ║         Repository Layer (DAL)                     ║
    ║                                                    ║
    ║    CRUD operations, data access abstraction        ║
    ║    IN/OUT: Immutable Domain Models ONLY            ║
    ║                                                    ║
    ╚══════════════════════┬═════════════════════════════╝
                           │
                           │ ORM Objects (SQLAlchemy)
                           │ (Mutable database models with live connection to db)
                           ▼
                    ┌──────────────────────┐
                    │   Data Layer         │
                    │   (SQLite Database)  │
                    │                      │
                    │   ORM + Converters   │
                    └──────────────────────┘


═══════════════════════════════════════════════════════
KEY COMMUNICATION BOUNDARIES
═══════════════════════════════════════════════════════

1. External ←→ API
   • Protocol: HTTP (JSON payloads)
   • Testing: HTTP/API integration tests

2. API ←→ Repository
   • Protocol: Immutable Domain Models (Pydantic)
   • IN: CreateCommand, UpdateCommand, query params
   • OUT: Domain entities or None
   • Testing: Repository layer tests (independent of HTTP)

3. Repository ←→ Data Layer
   • Protocol: ORM Objects (SQLAlchemy)
   • Repository converts Domain ↔ ORM explicitly
   • Never exposes ORM outside Data Layer


═══════════════════════════════════════════════════════
TESTING STRATEGY
═══════════════════════════════════════════════════════

🧪 Layer 1: HTTP/API Tests (tests/api/)
   • Test complete workflows via HTTP
   • Verify externally observable behavior
   • Use FastAPI TestClient

🧪 Layer 2: Repository Tests (tests/dal/)
   • Test data persistence independently
   • No HTTP layer involved
   • Direct repository method calls
